# Demo Script (for video + live)

Keep it ~3 min. Show grounding + speed.

1. **Hook (15s):** "Ever frozen on a question mid-presentation? Cue answers in your own words, instantly."
2. **Setup (20s):** Upload the prepared deck/docs → show ingestion + "indexed".
3. **Use case 1 — hard Q mid-talk (40s):** Start a session, simulate slide 5, type/ask a deep follow-up, press rescue → grounded script appears line-by-line; read it aloud.
4. **Use case 3 — live-sale slip (40s):** Feed transcript that says the wrong brand → periodic scan flags it → correction line appears.
5. **Under the hood (30s):** Show the architecture + the Loop Engineering diagram (planner/dev/QA) — "built by agents, context-engineered."
6. **Close (15s):** impact + what's next.

Record clean takes per segment; stitch. Capture latency on screen.
