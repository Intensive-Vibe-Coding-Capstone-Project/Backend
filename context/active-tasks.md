# Active Tasks (the loop's worklist)

> Planner fills this; Dev/QA work it; clear done items to progress.md.

## Today
- [ ] Confirm tech stack + log it.
- [ ] FastAPI skeleton + `.env` + lint + test runner.
- [ ] Choose vector store; install deps.

## Backlog (next up)
- [ ] `POST /documents` ingestion + parsers (pdf/docx/txt/pptx/epub).
- [ ] Chunking + Gemini embeddings + vector write/read.
- [ ] `POST /rescue` (text) → retrieve → Gemini → grounded script.
- [ ] Session model + history.
- [ ] Grounding eval set (10–15 Q/A).

## Format
`- [ ] <task> — owner — acceptance criteria`
